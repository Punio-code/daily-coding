package quiz;

public class Quiz {
    String text; // インスタンス変数
    boolean bool;

    public static void main(String[] args) {
        Quiz q = new Quiz();
        System.out.println(q.text + q.bool);
    }
}